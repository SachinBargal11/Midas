@model CAIdentityServer.Models.LoginCredential

@{
    ViewBag.Title = "Login Page";
    Layout = "~/Views/Shared/_LoginLayout.cshtml";
}
@using (Html.BeginForm())
{
    <div class="flex-box">
        <div class="inner">
            <h3 class="form-heading"><strong>MIDAS </strong>PORTAL</h3>
            <div class="alert-danger">
                @Html.ValidationSummary()
            </div>
            <div class="frm-block">
                <form>
                    <div class="form-group mb-1">
                        @Html.TextBoxFor(model => model.Username, new { id = "txtUserName", type = "email", Value = "", required = "required", @class = "form-control" })
                    </div>
                    <div class="form-group mb-3">
                        @Html.TextBoxFor(model => model.Password, new { id = "txtPassword", type = "password", Value = "", required = "required", @class = "form-control" })
                    </div>
                    @*<div class="form-check mb-3 keep">
                        @if (ViewBag.RememberMe == true)
                        {
                        <label class="form-check-label">
                            @Html.CheckBoxFor(model => model.RememberMe, new { id = "chkRememberMe", @checked = ViewBag.RememberMe,  @class ="form-check-input"})
                            Keep me signed in
                        </label>
                        }
                        else
                        {
                            <label class="form-check-label">
                                @Html.CheckBoxFor(model => model.RememberMe, new { id = "chkRememberMe", @class = "form-check-input" })
                                Keep me signed in
                            </label>
                        }
                    </div>*@
                    <a href="@Html.Raw(Html.AttributeEncode(ViewBag.FPURL))">Forgot Password?</a>
                    <button class="btn btn-success btn-block">Sign In</button>
                   
                </form>
            </div>
            <small class="form-text text-white text-center">Don't have an account? <a href="@Html.Raw(Html.AttributeEncode(ViewBag.REGURL))">SignUp</a></small>
            
        </div>
    </div>
}

